---
layout: default
---

{% if page.cv_pdf %}
<iframe 
  {% if page.cv_pdf contains '://' %}
    src="{{ page.cv_pdf }}"
  {% else %}
    src="{{ page.cv_pdf | prepend: 'assets/pdf/' | relative_url }}"
  {% endif %}
  width="100%" height="900px" 
  style="border:none;"
  sandbox>
  This browser does not support PDFs. Please download the PDF to view it: <a href="{{ '/assets/pdf/cv.pdf' | relative_url }}">Download CV</a>

</iframe>
{% else %}
<p>No CV pdf uploaded yet. Hang tight!</p>

{% endif %}